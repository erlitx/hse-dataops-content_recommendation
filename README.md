# 🎬 Recommender System (MLOps Project)

Учебный проект по созданию системы персональных рекомендаций фильмов / товаров / новостей.  
Цель — разработать промышленный MLOps-пайплайн, включающий обработку данных, обучение модели, CI/CD, контейнеризацию и мониторинг качества.

---

# 📌 Описание проекта

Система рекомендаций анализирует поведение пользователей и формирует индивидуальные рекомендации.  
Используемые подходы:

- **Collaborative Filtering**
- **Content-Based Filtering**
- **Hybrid Approach**

В проекте реализованы MLOps-принципы:

- автоматизированные пайплайны подготовки данных и фичей
- MLflow для трекинга экспериментов и моделей
- Docker-контейнеры для воспроизводимости
- CI/CD через GitHub Actions
- A/B-тестирование
- мониторинг качества

---

# 📊 Используемые данные

MovieLens 1M Dataset  
https://grouplens.org/datasets/movielens/

```
data/
 ├── external/
 ├── raw/
 └── processed/
```

---

# Архитектура проекта

```
recommender-system/
│  
├── data/
├── notebooks/
├── src/
│   ├── data/
│   ├── features/
│   ├── models/
│   ├── evaluation/
│   └── utils/
├── docker/
├── mlflow/
├── ci/
├── config/
├── requirements.txt
└── README.md
```

## Запуск приложения
<code>
pip install -r requirements.txt
</code>
<code>
uvicorn app.main:app --reload
</code>

## API Docs
http://localhost:8000/docs

## Тесты
pytest tests/

## Docker
docker build -t movielens-recommender .
docker run -p 8000:8000 movielens-recommender

### Запуск

# Клонировать/создать структуру
pip install -r requirements.txt
uvicorn app.main:app --reload

# Тесты
pytest tests/ -v

# Docs: http://localhost:8000/docs




# Стратегия ветвления

В данном MLOps проекте используется классический **GitHub Flow**:

- `main` - всегда стабильная, готовая к продакшену версия
- Feature branches - создаются от `main` для новых фич/улучшений
- Pull Request + Code Review для всех изменений
- Автоматические тесты через GitHub Actions
- Семантическое версионирование (SemVer) для релизов

### Процесс разработки:
1. Создать feature branch: `git checkout -b feature/имя_ветки`
2. Разработать фичу, коммитить с Conventional Commits
3. Создать Pull Request в `main`
4. Пройти code review и CI проверки
5. Смержить PR
6. Создать релизный тег при необходимости
---

# Быстрый старт

## Установка зависимостей
pip install -r requirements.txt

## Обработка данных
python src/data/make_dataset.py  
python src/features/build_features.py

## Обучение модели
python src/models/train_model.py --config config/config.yaml

## Запуск API
uvicorn src.models.predict_model:app --host 0.0.0.0 --port 8000

---

# Метрики
- Precision@K  
- Recall@K  
- MAP@K  
- NDCG  

---

# 🐳 Docker
docker build -t recommender-service -f docker/Dockerfile .  
docker run -p 8000:8000 recommender-service

---

# 🔁 CI/CD
GitHub Actions pipeline (`ci/pipeline.yml`)

---

# 👀 Мониторинг
Дрейф данных и качества, алерты, rollback.

---

MovieLens Recommender Service
User Story
Название: Рекомендации фильмов для пользователей стриминговых сервисов
Как пользователь стримингового сервиса (например, Кирилл), я хочу получить персонализированные рекомендации фильмов, чтобы быстрее найти интересный контент для просмотра вечером.

Сценарий использования:

1. Кирилл заходит в мобильное приложение стримингового сервиса

2. На главной странице видит блок "Рекомендации для вас"

3. Приложение отправляет его ID на сервер рекомендаций

4. Получает список из 10 фильмов с ID и получает их названия из базы

5. Кирилл выбирает фильм и смотрит


Функциональные требования
Входные данные: JSON с полями user_id (int, ≥1), top_n (int, 1-50), model_type ("baseline"|"content")

Выходные данные: JSON с полями user_id, model_type, top_n, recommendations (список movieId), count

Валидация: Pydantic автоматическая валидация, 422 при ошибках

Модель: Загружается MovieLens Small при старте, поддержка 2 алгоритмов

Health check: GET /health всегда возвращает {"status": "ok"}
